# bre-og-fjell-fontpage-element

## Install
You need sass to compile the sass to css
```
gem install sass
```

### Compile

'cd' in to the directory and run
```
sass style.scss style.css
```
To compile the css.
